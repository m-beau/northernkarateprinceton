{
  "name": "nks-princeton",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "dev:wrangler": "astro build && wrangler pages dev dist --compatibility-date=2024-01-01",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.60.0"
  }
}
